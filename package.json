{
  "name": "yue",
  "private": true,
  "version": "0.2.0",
  "description": "JyutTranslate — English ↔ Cantonese live translator (PWA + WordPress)",
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web -- --host 0.0.0.0 --port 5173",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:tunnel": "npx --yes cloudflared tunnel --url http://localhost:5173",
    "cloud:install": "./scripts/cloud-agent-install.sh",
    "cloud:start": "./scripts/cloud-agent-start.sh",
    "build:web": "node scripts/generate-icons.mjs && npm run build --prefix apps/web",
    "build:web:marketing": "node scripts/generate-icons.mjs && VITE_BASE_PATH=./ npm run build --prefix apps/web && rm -rf dist-marketing && mkdir -p dist-marketing && cp -R apps/web/dist/. dist-marketing/",
    "build:web:wp": "node scripts/generate-icons.mjs && VITE_BASE_PATH=./ npm run build --prefix apps/web && rm -rf wordpress/yue-translator/app && mkdir -p wordpress/yue-translator/app && cp -R apps/web/dist/. wordpress/yue-translator/app/",
    "icons": "node scripts/generate-icons.mjs",
    "smoke:canto": "npm run smoke:canto --prefix apps/api",
    "test:translate": "node scripts/translation-quality-bot.mjs",
    "docs:screenshots": "node scripts/capture-docs-screenshots.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
