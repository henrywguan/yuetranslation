import { JadeGlassField } from '../components/JadeGlassField'

/** Marketing background — refractive jade/harbor glass field. */
export function ShaderBackground() {
  return <JadeGlassField variant="marketing" />
}
