import { JadeGlassField } from './JadeGlassField'

/** Translator shell background — same refractive glass field as marketing. */
export function FluidBackground() {
  return <JadeGlassField variant="app" />
}
