# Compiled React Email templates

Generated by `node scripts/compile-api-emails.mjs` (also `npm run compile:api-emails` / Vercel `build:vercel`).

Do not edit these `.js` files by hand — change the sibling `../*.tsx` sources and recompile.

`notify.ts` loads these modules only when sending mail so `/api/health` never imports JSX.
